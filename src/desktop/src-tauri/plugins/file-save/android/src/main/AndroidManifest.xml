<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider android:name="ai.alook.plugin.filesave.FileShareProvider"
            android:authorities="${applicationId}.file-share"
            android:exported="false" android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/alook_file_share_paths" />
        </provider>
    </application>
</manifest>
