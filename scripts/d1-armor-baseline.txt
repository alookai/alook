# D1-armor baseline — the "swallow-class / bare-getDb" burn-down list (PR-C3① ratchet).
#
# Each line is a community route.ts that calls getDb() WITHOUT a D1-armor carrier
# (withD1Retry / readOrStale / idempotentWrite / nonIdempotentWriteAllowed). These
# are the PRE-EXISTING bare routes at C3①'s introduction — grandfathered so the lint
# can land now (a hard rule would fail all 92 at once). scripts/lint-d1-armor.mjs
# fails a NEW bare route that is not in this list.
#
# RATCHET — this list may only SHRINK, never grow:
#   - PR-C2 armors each route (wraps its D1 exec in a carrier) and DELETES its line here.
#   - When this list hits 0 entries, C2 is done and the baseline file can be removed.
#   - The lint enforces monotonicity: adding a line (letting a new bare route in) fails
#     the line-count check; a line whose file is no longer bare must be removed.
#
# Lines starting with '#' are comments. One repo-relative path per remaining line.
